#ifndef UTILS_H
#include "utils.h"
#endif // UTILS_H

template<typename T>
class createDepList
{
    void createLists(btree<T> tree);
};

template<typename T>
void createDepList<T>::createLists(btree<T> tree)
{

}
